package edu.ntu.cltk.algo;

public class MatrixUtil {

	/**
	 * Check if the matrix is null or size is zer
	 * @param <T>o
	 * @param matrix
	 * @return
	 */
	public static<T> boolean isEmpty(T[][] matrix){
		if (matrix == null || matrix.length < 1)	return true;
		return (matrix.length < 1);
	}
	
	public static boolean isEmpty(int[][] matrix){
		if (matrix == null || matrix.length < 1)	return true;
		return (matrix.length < 1);
	}
	
	public static boolean isEmpty(byte[][] matrix){
		if (matrix == null || matrix.length < 1)	return true;
		return (matrix.length < 1);
	}
	
	public static boolean isEmpty(boolean[][] matrix){
		if (matrix == null || matrix.length < 1)	return true;
		return (matrix.length < 1);
	}
	
	public static boolean isEmpty(short[][] matrix){
		if (matrix == null || matrix.length < 1)	return true;
		return (matrix.length < 1);
	}

	public static boolean isEmpty(long[][] matrix){
		if (matrix == null || matrix.length < 1)	return true;
		return (matrix.length < 1);
	}
	
	public static boolean isEmpty(float[][] matrix){
		if (matrix == null || matrix.length < 1)	return true;
		return (matrix.length < 1);
	}
	public static boolean isEmpty(double[][] matrix){
		if (matrix == null || matrix.length < 1)	return true;
		return (matrix.length < 1);
	}
	public static boolean isEmpty(char[][] matrix){
		if (matrix == null || matrix.length < 1)	return true;
		return (matrix.length < 1);
	}
	public static boolean isEmpty(String[][] matrix){
		if (matrix == null || matrix.length < 1)	return true;
		return (matrix.length < 1);
	}
}
